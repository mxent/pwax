{
    "name": "@mxent/pwax",
    "version": "2.1.4",
    "private": true,
    "description": "Client runtime for the mxent/pwax Laravel package. Built to dist/pwax.js and shipped inside the Composer package — npm install is only needed to contribute.",
    "license": "MIT",
    "type": "module",
    "scripts": {
        "build": "node build.js",
        "dev": "node build.js --watch",
        "test": "vitest run",
        "test:watch": "vitest",
        "lint": "eslint src/js build.js",
        "lint:fix": "eslint --fix src/js build.js",
        "format": "prettier --write \"src/js/**/*.js\" build.js",
        "format:check": "prettier --check \"src/js/**/*.js\" build.js"
    },
    "devDependencies": {
        "esbuild": "^0.25.0",
        "eslint": "^9.15.0",
        "globals": "^15.12.0",
        "jsdom": "^25.0.1",
        "prettier": "^3.3.3",
        "vitest": "^4.1.10"
    },
    "engines": {
        "node": ">=20"
    }
}
